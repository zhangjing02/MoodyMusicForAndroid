<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer Dark Shadow (Ambient) -->
    <item>
        <shape android:shape="rectangle">
            <padding android:bottom="4dp" android:right="2dp" />
            <solid android:color="#20000000" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    
    <!-- Wooden Frame (Outer) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/classroom_wood_dark" />
            <corners android:radius="8dp" />
        </shape>
    </item>

    <!-- Wooden Frame (Light Profile) -->
    <item android:bottom="2dp" android:left="2dp" android:right="2dp" android:top="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/classroom_wood_medium" />
            <corners android:radius="6dp" />
        </shape>
    </item>

    <!-- Chalkboard Surface (Inner) -->
    <item android:bottom="8dp" android:left="8dp" android:right="8dp" android:top="8dp">
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:endColor="@color/classroom_chalkboard_edge"
                android:startColor="@color/classroom_chalkboard_main"
                android:type="linear" />
            <corners android:radius="2dp" />
            <stroke android:width="1dp" android:color="#30000000" />
        </shape>
    </item>

    <!-- Blackboard Header/Logo (Simulated) -->
    <item android:bottom="20dp" android:left="20dp" android:right="20dp" android:top="20dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="1dp" android:color="#10FFFFFF" android:dashGap="4dp" android:dashWidth="4dp" />
        </shape>
    </item>
</layer-list>
